import type { ReactNode } from 'react';
import {Link,NavLink} from 'react-router-dom'; import {useHealth} from '../hooks/useHealth.js';
export function Layout({children}:{children:ReactNode}){const down=useHealth();const nav=[['/','Dashboard'],['/developers','Developers'],['/technologies','Technologies'],['/projects','Projects'],['/explorer','Graph Explorer']];return <div className="min-h-screen"><header className="border-b bg-white"><div className="max-w-7xl mx-auto px-5 h-16 flex items-center justify-between"><Link to="/" className="font-bold text-lg">DevGraph</Link><nav className="flex gap-1">{nav.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`px-3 py-2 rounded-lg text-sm ${isActive?'bg-slate-100 font-semibold':'text-slate-500 hover:text-slate-900'}`}>{label}</NavLink>)}</nav></div></header>{down&&<div className="bg-amber-50 border-b border-amber-200 text-amber-800 text-sm px-5 py-2 text-center">CognoDB is currently unavailable. Database-backed features may be unavailable.</div>}<main className="max-w-7xl mx-auto px-5 py-8">{children}</main></div>}
